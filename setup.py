from setuptools import setup

setup(name='pacman_gym',
      version='0.0.2',
      install_requires=[
            'gymnasium==1.2.3',
            "networkx"
      ] # And any other dependencies required
)